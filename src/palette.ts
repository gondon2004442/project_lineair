/**
 * Палитра объекта. Только эти цвета.
 *
 * Основа взята из дизайн-документа BESTIARIUM один в один, без подгонки
 * на глаз: шкала бетона, акцентный красный, служебный жёлтый, чернила.
 * Среду добавляет офисный кит (docs/art/lineair_office.py) — дерево,
 * ковролин, латунь, стекло.
 *
 * Второе правило брифа: различай по СВЕТЛОТЕ, а не по цвету. Оттенок тут
 * почти ничего не сообщает, светлота сообщает всё. Отсюда разброс от
 * #14161A до #D8DBDE и запрет на оливковый с коричневым в базе; тёплый
 * кит остаётся только для офисных секторов.
 *
 * Правило, которое задал дизайн-документ и которое нельзя нарушать:
 * КРАСНЫЙ — ТОЛЬКО ИГРОК. Ни один сотрудник, ни один предмет обстановки
 * красным не красится. Иначе силуэт субъекта перестаёт читаться с одного
 * взгляда, а на этом держится всё.
 */
export const PALETTE = {
  // --- Шкала бетона: чем выше должность, тем меньше осталось кожи ---
  /** CONCRETE/900 — самый тёмный бетон. */
  concrete900: 0x14161a,
  /** CONCRETE/700 */
  concrete700: 0x2a2e33,
  /** CONCRETE/500 */
  concrete500: 0x6b6f75,
  /** CONCRETE/300 */
  concrete300: 0x9aa0a6,
  /** CONCRETE/100 — почти белый. */
  concrete100: 0xd8dbde,

  /** ACCENT/RED — только субъект. */
  red: 0xc8102e,
  /** SERVICE/YELLOW — таблички, разметка, телеграфы. */
  yellow: 0xe8b923,
  /** INK/BLACK — фон за пределами помещения. */
  black: 0x0a0b0d,

  // --- Игровые поверхности: различаются светлотой, а не цветом ---
  /** Стена. Темнее пола: пол светится, стены держат рамку. */
  wall: 0x1f2226,
  /** Пол. */
  floor: 0x3a3f44,
  /** Шов между плитками пола. */
  floorSeam: 0x2a2e33,
  /** Мебель и перегородки. */
  furniture: 0x4a4e54,

  // --- Тёплый офисный кит: только офисные сектора, как контраст бетону ---
  /** Ореховая обшивка стены. */
  wood: 0x6b4f32,
  /** Тёмный дуб: цоколь, корпус стеллажа. */
  woodDark: 0x40301e,
  /** Латунный профиль между обшивкой и бетоном. */
  brass: 0x8a7038,
  /** Ковролин. */
  carpet: 0x464539,
  /** Шов между плитками ковролина. */
  carpetDark: 0x32311f,
  /** Обивка кресел и перегородок. */
  fabric: 0x6b665a,
  /** Стекло перегородок. */
  glass: 0xa9bcc0,
  /** Бумага, папки. */
  paper: 0xc9ccc8,
  /** Фикус. */
  leaf: 0x2e4a33,
  /** Корешки папок. */
  binder: 0x7a4b3a,
} as const;

export type PaletteColor = (typeof PALETTE)[keyof typeof PALETTE];

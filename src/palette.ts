/**
 * Палитра объекта. Только эти цвета.
 * Бетон (четыре градации), чёрный, красный-акцент, служебный жёлтый.
 * Ни скруглений, ни градиентов, ни «мягких» цветов.
 */
export const PALETTE = {
  /** Фон за пределами помещения. */
  black: 0x0d0d0c,
  /** Пол. */
  concreteDark: 0x181816,
  /** Разметка пола, тень блока. */
  concrete: 0x2b2b27,
  /** Лицевая грань бетонного блока. */
  concreteMid: 0x45453e,
  /** Субъект, служебный текст. */
  concreteLight: 0xa9a89d,
  /** Акцент: заражённое, урон, кровь объекта. */
  red: 0xc4122f,
  /** Служебный жёлтый: боеприпас субъекта, телеграф, отладка. */
  yellow: 0xe8b21a,
} as const;

export type PaletteColor = (typeof PALETTE)[keyof typeof PALETTE];
